# P4-01 acceptance evidence

Status: **PASS**  
Accepted at: `2026-09-22T19:01:01Z`  
Scope: bounded v1 semantic verification of supplied, hash-pinned evidence

## Accepted claims

- `ctc verify` rebuilds the P3 contract before evaluating a result and rejects stale, forged, missing, or extra identities.
- Project-candidate verification binds a clean inspected baseline to a separate descendant Git worktree and derives the candidate revision, dirty digest, changed paths, ignored source evidence, and source-snapshot digest from that worktree.
- No-follow preflight rejects source-sensitive file or directory symlinks in either the baseline or candidate, including tracked, untracked, ignored, and broken links.
- Registered visual metrics enforce their unit and numeric range. Unsupported measurements block; a measured budget miss fails.
- PASS requires exact requirement, context, domain, evidence, comparison-pair, artifact, and summary coverage with no defects or blockers.
- Blocking contract conditions remain visible as structured blockers even when measured evidence also proves a failure.
- Reports are deterministic, scoped, and cannot overwrite protected inputs or escape through a symlink.

## Executed evidence

- Focused semantic-verifier suite: `node --test tests/unit/v1_semantic_validator_cli.test.js`
  - 27 tests passed, 0 failed.
- P1-P3 focused suites remained green before the final P4 review: 37 tests passed, 0 failed.
- JavaScript syntax checks passed for the inspector, verifier, CLI, and focused test.
- All v1 JSON schemas parsed.
- `git diff --check` passed.

The focused negative controls include missing evidence, measured visual failure, out-of-range and wrong-unit metrics, unsupported metrics, stale and mismatched evidence, forged P3 namespaces, omitted candidate paths, dirty baselines, unrelated and divergent repositories, ignored out-of-scope assets, source symlinks, report-path attacks, and both historical false-green visual reports.

## Independent review

- Astra initially rejected P4 because source-sensitive tracked and ordinary untracked symlinks were not covered by candidate provenance. After no-follow baseline/candidate preflight and two regression controls were added, Astra returned PASS and independently reran 27/27 tests.
- Luna independently exercised metric-range and omitted-path controls, then retested the corrected revision with ignored broken source symlinks in both baseline and candidate. Both were rejected with exit `3`; Luna returned final PASS after 27/27 focused tests.
- Grok Build rejected the integrated candidate once because ignored source files beneath descendant directories literally named `build` or `.gradle` were filtered out of provenance. The classifier now excludes those roots only when they precede any real `src` segment. New omitted-evidence controls cover ignored Kotlin and asset files beneath both descendant names. Grok, Astra, and Luna each returned PASS on the correction; the final integrated v1 suite passed 64/64.

## What this does not prove

This PASS does not prove that an Android app builds, preserves behavior, matches a Claude Design, runs on DC-1, or is release-ready. It proves that the bounded v1 verifier evaluates supplied evidence fail-closed and that its synthetic adversarial controls behave as specified. Real project acceptance still requires actual build, runtime behavior, render, comparison, and device evidence named by the contract.
