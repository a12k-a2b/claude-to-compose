# P5-01 delivery evidence

Status: **PASS**  
Accepted at: `2026-09-22T19:31:16Z`  
Published branch: `codex/design-retrofit-v1`  
Published implementation revision: `68b583684f78b1e40d4d076f16c78a93fe0a2c74`

## Delivered workflow

- Local `ctc` CLI with doctor, workspace initialization, read-only Android inspection, retrofit-contract construction, coding-agent packet generation, and fail-closed semantic verification.
- Versioned JSON schemas for app models, correspondence and behavior manifests, owner approvals, retrofit contracts, agent packets, and verification results.
- Immutable inspected-baseline plus separate descendant Git-worktree candidate architecture.
- Deterministic `PASS`, `FAIL`, `BLOCKED`, invalid-input, and infrastructure exit semantics.
- Quickstart, frozen contract, architecture gap analysis, and end-to-end project plan.
- No Railway, hosted service, production account, or live customer data dependency.

## Final verification

- `npm run test:v1`: 64 passed, 0 failed.
- Focused semantic verifier: 27 passed, 0 failed.
- Fail-closed visual/fidelity foundation subset: 30 passed, 0 failed.
- `node bin/ctc.js doctor --json`: PASS for local Node, Git, and schema availability; no Android project or Gradle task was evaluated.
- `npm pack --dry-run --json` with an isolated writable npm cache: PASS; all three published bins were present.
- V1 schemas parsed as JSON, CLI help rendered, executable modes were present, and `git diff --check` passed.

The broader legacy unit command completed with 315 passed and 9 failed. Those nine are retained as non-v1 blockers rather than counted green: one localhost listener is forbidden by the execution sandbox, one auto-tuner test lacks `output/test_da63/rendered_compose.png`, and seven vector-linter checks depend on the absent `output/test_da63/design_spec.json` or its downstream output. The bounded v1 suites do not depend on those missing artifacts.

## Independent review trail

- Astra directed the architecture and returned final PASS after independently rerunning the P4 gate and the last ignored-source regression.
- Luna independently attacked metric ranges, omitted candidate changes, baseline/candidate symlinks, ignored assets, and descendant `build`/`.gradle` source paths; final verdict PASS.
- Grok Build first rejected candidate provenance for ignored files beneath real source descendants named `build` or `.gradle`. The classifier and regression tests were corrected; Grok re-read the fix and returned PASS.
- Sol implemented the bounded phases and performed a final command/document/package audit. Four documentation/metadata defects were corrected; final recheck PASS.

## Publication

The implementation commit was pushed to:

`https://github.com/a12k-a2b/claude-to-compose/tree/codex/design-retrofit-v1`

This evidence commit is pushed on the same branch after the implementation publication was confirmed.

## Remaining work

This is a testable v1 of the agent handoff and evidence contract, not a completed Note Overlay redesign. The first real pilot still requires the selected Note Overlay repository/revision and Claude Design links. Actual project acceptance must then include real Gradle/build artifacts, deterministic behavior scenarios, native renders, comparison metrics, and the intended DC-1 profile. Motion/animation requires a later timeline runner with controlled clocks and does not inherit a PASS from static UI evidence.
