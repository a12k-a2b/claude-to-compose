# P1-01 acceptance — versioned retrofit artifact contract

Outcome: `PASS`

Accepted at: `2026-09-22T16:31:59Z`

## Proved

- `ExistingAppModel`, `RetrofitContract`, and `VerificationResult` use fixed v1 schemas and explicit claim scopes.
- A truthful pre-build `BLOCKED` result does not require an invented candidate artifact hash; `PASS` does.
- The retrofit contract freezes a typed, hash-pinned acceptance matrix with requirements, scenarios, contexts, domains, configurations, and numeric budgets.
- Verification evidence has typed scene, scenario, state, viewport, configuration, runtime, source-snapshot, and subject-artifact identity.
- Owner approvals are hashed inputs, implementation paths exclude the repository root and traversal, and requirement evidence lists cannot be empty.
- The CLI exit contract is fixed at `0 PASS`, `1 FAIL`, `2 BLOCKED`, `3 invalid input`, and `4 infrastructure failure`.

## Evidence

- Manager command: `node --test tests/unit/v1_contract_schema.test.js`
- Result: 13 tests passed, 0 failed.
- Manager command: `git diff --check`
- Result: passed.
- Astra architecture keeper: `PASS — accept P1-01 and begin P2`.
- Luna independent reviewer: `PASS for P1-01 contract acceptance`.

## Boundary

This accepts the artifact and CLI outcome contract only. The test helper named `semanticContractCheck` is a fixture oracle, not production validation. Cross-artifact hash/reference/pair/count enforcement remains mandatory P4 work, and `docs/v1/V1_CONTRACT.md` forbids a project `PASS` until that validator exists.
