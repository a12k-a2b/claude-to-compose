# P2-01 acceptance — read-only existing-app inspection

Outcome: `PASS`

Accepted at: `2026-09-22T17:15:37Z`

## Proved

- The separate local `ctc` entrypoint provides `doctor`, `init`, and lexical `inspect-app` commands without invoking Gradle, Java, Playwright, or the legacy generation workflow.
- Inspection emits a schema-valid `ExistingAppModel` with Git revision provenance, bounded lexical modules and symbols, uncertainties, evidence references, test clues, and explicitly unexecuted build-command clues.
- Repository-controlled Git helpers are disabled; staged, unstaged, and untracked changes contribute to the dirty-tree digest; symlink targets are not followed; unsupported special files fail closed.
- Workspace and output containment use canonical paths. Final output symlinks are rejected and output writes are atomic and no-follow.
- Remote credentials, query strings, and fragments are excluded from recorded provenance.
- Constructor parameters are not mistaken for inheritance clauses, and ignored/orphan/comment/string fixtures are not promoted to production symbols.
- No-Git inspection returns `BLOCKED`; invalid paths return invalid input; output is one JSON document with the documented exit semantics.

## Evidence

- Manager command: `node --test tests/unit/v1_inspect_cli.test.js tests/unit/v1_contract_schema.test.js`
- Result: 24 tests passed, 0 failed.
- Manager commands: `node --check retrofit/inspect_app.js`, `node --check retrofit/cli.js`, and `git diff --check`.
- Result: passed.
- Astra architecture keeper: `PASS — accept P2 and begin P3` after independently rerunning 24/24 tests.
- Luna independent reviewer: `PASS` after independently rerunning the inspection and schema suites.
- Grok Build adversarial review rejected revision 1 with three concrete security/correctness defects; revision 2 contains the requested fixes and regression tests. The follow-up Grok process re-read the fixes but hit its local session turn/bootstrap limit before emitting a second verdict, so it is not counted as an acceptance vote.

## Boundary

This accepts bounded, local, lexical evidence collection. It is not a complete Kotlin or Gradle semantic model, does not execute or prove build readiness, and does not prove runtime behavior. A project-level `PASS` remains forbidden until the P4 production semantic validator evaluates complete evidence.
